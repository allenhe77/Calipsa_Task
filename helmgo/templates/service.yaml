apiVersion: v1
kind: Service
metadata: 
  namespace: monitoring
  name: {{.Release.Name}}
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/port: '2112'
spec:
  type: NodePort
  selector:
    app: golang
  ports:
    - name: main
      protocol: TCP
      port: 80
      targetPort: {{.Values.goport}}
      nodePort: 30001

